# Omega-S : Core dependencies
# Tested with Python 3.10+

# Deep learning
torch>=2.1.0
torchvision>=0.16.0

# LLM / LoRA integration
transformers>=4.40.0
peft>=0.10.0

# Distributed training
# (FSDP is bundled with PyTorch >= 2.1)

# Experiments and benchmarks
numpy>=1.24.0
pandas>=2.0.0
tabulate>=0.9.0   # for df.to_markdown()

# Optional: orchestration experiment
# simpy>=4.1.0

# Optional: experiment tracking
# wandb
# tensorboard
